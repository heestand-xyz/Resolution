// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "Resolution",
    products: [
        .library(
            name: "Resolution",
            targets: ["Resolution"]),
    ],
    targets: [
        .target(
            name: "Resolution",
            dependencies: [])
    ]
)
